[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    -ra
    --strict-markers
    --strict-config
    --tb=short
markers =
    functional: Tests that invoke external tools (php, composer, xmllint). Skipped automatically if the tool is not present.
    snapshot: Snapshot tests using syrupy.
